CREATE TABLE IF NOT EXISTS caller (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    number TEXT NOT NULL
);

getAllCallers:
SELECT * FROM caller;

insertCaller:
INSERT INTO caller(name, number) VALUES (?, ?);

deleteCallerById:
DELETE FROM caller WHERE id = ?; 